# auto-generated by grc.converter

id: timing_utils_system_time_tagger
label: System Time Tagger
category: '[Sandia]/Timing Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [c, f, s, b]
    option_labels: [Complex, Float, Short, Byte]
    option_attributes:
        type: [complex, float, short, byte]
    hide: part
-   id: interval
    label: Tag Interval
    dtype: int
    default: '10000'
    options: ['0']
    option_labels: [Disabled]

inputs:
-   domain: stream
    dtype: ${ type.type }

outputs:
-   domain: stream
    dtype: ${ type.type }

templates:
    imports: |-
        import timing_utils
        import pmt
    make: timing_utils.system_time_tagger_${type}(${interval})

file_format: 1
