# auto-generated by grc.converter

id: timing_utils_uhd_timed_pdu_emitter
label: UHD Timed PDU Emitter
category: '[Sandia]/Timing Utilities'

parameters:
-   id: rate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: late_pdu_mode
    label: Drop Late?
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: set
    optional: true

outputs:
-   domain: message
    id: trig
    optional: true

templates:
    imports: import timing_utils
    make: timing_utils.uhd_timed_pdu_emitter(${rate}, ${late_pdu_mode})

file_format: 1
