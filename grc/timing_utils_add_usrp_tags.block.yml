# auto-generated by grc.converter

id: timing_utils_add_usrp_tags
label: Add USRP Tags
category: '[Sandia]/Timing Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [c, f, i, s, b]
    option_labels: [Complex, Float, Int, Short, Byte]
    option_attributes:
        type: [complex, float, int, short, byte]
    hide: part
-   id: freq
    label: Frequency
    dtype: float
-   id: rate
    label: Rate
    dtype: float
-   id: ti
    label: Time (Secs)
    dtype: int
    hide: part
-   id: tf
    label: Time (Frac)
    dtype: float
    hide: part
-   id: tag_now
    label: Tag Now
    dtype: int
    default: '0'
    hide: part

inputs:
-   domain: stream
    dtype: ${ type.type }

outputs:
-   domain: stream
    dtype: ${ type.type }

templates:
    imports: import timing_utils
    make: timing_utils.add_usrp_tags_${type}(${freq}, ${rate}, ${ti}, ${tf})
    callbacks:
    - tag_now(${tag_now})

file_format: 1
