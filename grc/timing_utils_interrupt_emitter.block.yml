# auto-generated by grc.converter

id: timing_utils_interrupt_emitter
label: Interrupt Emitter
category: '[Sandia]/Timing Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [c, f, i, s, b]
    option_labels: [Complex, Float, Int, Short, Byte]
    option_attributes:
        type: [complex, float, int, short, byte]
    hide: part
-   id: rate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: late_pdu_mode
    label: Drop Late?
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: ${ type.input }
-   domain: message
    id: set
    optional: true

outputs:
-   domain: message
    id: trig
    optional: true

templates:
    imports: import timing_utils
    make: timing_utils.interrupt_emitter_${type}(${rate}, ${late_pdu_mode})

file_format: 1
