# auto-generated by grc.converter

id: timing_utils_interrupt_emitter
label: Interrupt Emitter
category: '[Sandia]/Timing Utilities'

parameters:
-   id: rate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: late_pdu_mode
    label: Drop Late?
    dtype: bool
    default: 'False'

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: set
    optional: true

outputs:
-   domain: message
    id: trig
    optional: true

templates:
    imports: import timing_utils
    make: timing_utils.interrupt_emitter(${rate}, ${late_pdu_mode})

documentation: |-
    Block outputs a message every interval seconds according to system clock.  It does, however, use the input stream tags as a reference to adjust times.

file_format: 1
