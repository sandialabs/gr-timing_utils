# auto-generated by grc.converter

id: timing_utils_uhd_timed_cordic_emulator
label: Timed CORDIC Emulator
category: '[Sandia]/Timing Utilities'

parameters:
-   id: short
    label: File Type
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: [Complex, Short]
-   id: filename
    label: Filename
    dtype: file_open
-   id: replay
    label: Replay File?
    dtype: enum
    default: 'True'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
    hide: part
-   id: decimation
    label: Decimation
    dtype: int
-   id: samp_rate
    label: Sample Rate
    dtype: float
-   id: f0
    label: Initial Freq
    dtype: float
    hide: part
-   id: epoch
    label: Start Time
    dtype: float
    default: '0'
    hide: part

inputs:
-   domain: message
    id: command
    optional: true

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: |-
            import timing_utils
            import sandia_utils
    make: timing_utils.uhd_timed_cordic_emulator(${filename}, ${replay}, ${decimation},
        ${samp_rate}, ${f0}, ${short}, ${epoch})

file_format: 1
