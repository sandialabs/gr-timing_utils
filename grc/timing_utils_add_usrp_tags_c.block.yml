# auto-generated by grc.converter

id: timing_utils_add_usrp_tags_c
label: Add USRP Tags
category: '[Sandia]/Timing Utilities'

parameters:
-   id: freq
    label: Frequency
    dtype: float
-   id: rate
    label: Rate
    dtype: float
-   id: ti
    label: Time (Secs)
    dtype: int
    hide: part
-   id: tf
    label: Time (Frac)
    dtype: float
    hide: part
-   id: tag_now
    label: Tag Now
    dtype: int
    default: '0'
    hide: part

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import timing_utils
    make: timing_utils.add_usrp_tags_c(${freq}, ${rate}, ${ti}, ${tf})
    callbacks:
    - tag_now(${tag_now})

file_format: 1
