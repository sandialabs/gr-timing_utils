# auto-generated by grc.converter

id: timing_utils_edge_detector_bb
label: Edge Detector
category: '[Sandia]/Timing Utilities'

parameters:
-   id: mode
    label: Edge Mode
    dtype: enum
    options: [timing_utils.RISING_EDGE, timing_utils.FALLING_EDGE, timing_utils.ANY_EDGE]
    option_labels: [Rising, Falling, Any]

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import timing_utils
    make: timing_utils.edge_detector_bb(${mode})

documentation: |-
    This block accepts a binary input stream of data and will output zeros except when an specified edge is detected.

file_format: 1
