# auto-generated by grc.converter

id: timing_utils_timed_freq_xlating_fir
label: Timed Frequency Xlating FIR
category: '[Sandia]/Timing Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [ccc, ccf, fcc, fcf, scc, scf]
    option_labels: [Complex->Complex (Complex Taps), Complex->Complex (Real Taps),
        Float->Complex (Complex Taps), Float->Complex (Real Taps), Short->Complex
            (Complex Taps), Short->Complex (Real Taps)]
    option_attributes:
        input: [complex, complex, float, float, short, short]
        output: [complex, complex, complex, complex, complex, complex]
        taps: [complex_vector, real_vector, complex_vector, real_vector, complex_vector,
            real_vector]
    hide: part
-   id: decim
    label: Decimation
    dtype: int
    default: '1'
-   id: taps
    label: Taps
    dtype: ${ type.taps }
-   id: center_freq
    label: Center Frequency
    dtype: real
    default: '0'
-   id: samp_rate
    label: Sample Rate
    dtype: real
    default: samp_rate
-   id: tag_key
    label: Frequency Tag Key
    dtype: string
    default: '"set_freq"'

inputs:
-   domain: stream
    dtype: ${ type.input }
-   domain: message
    id: freq
    optional: true

outputs:
-   domain: stream
    dtype: ${ type.output }

templates:
    imports: |-
        import timing_utils
        from gnuradio.filter import firdes
    make: timing_utils.timed_freq_xlating_fir_${type}(${decim}, ${taps}, ${center_freq},
        ${samp_rate},${tag_key})
    callbacks:
    - set_taps(${taps})
    - set_center_freq(${center_freq})
    - set_decim(${decim})

file_format: 1
