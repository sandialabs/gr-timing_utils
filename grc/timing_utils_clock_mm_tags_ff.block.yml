# auto-generated by grc.converter

id: timing_utils_clock_mm_tags_ff
label: Timed Clock Recovery MM
category: '[Sandia]/Timing Utilities'

parameters:
-   id: omega
    label: Omega
    dtype: real
    default: samp_per_sym*(1+0.0)
-   id: gain_omega
    label: Gain Omega
    dtype: real
    default: 0.25*0.175*0.175
-   id: mu
    label: Mu
    dtype: real
    default: '0.5'
-   id: gain_mu
    label: Gain Mu
    dtype: real
    default: '0.175'
-   id: omega_relative_limit
    label: Omega Relative Limit
    dtype: real
    default: '0.005'
-   id: tag_interval
    label: Tag Interval
    dtype: int
    default: '10000'
    hide: part

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import timing_utils
    make: timing_utils.clock_mm_tags_ff(${omega}, ${gain_omega}, ${mu}, ${gain_mu},
        ${omega_relative_limit}, ${tag_interval})

documentation: |-
    This block is identical to the in-tree MM block, however it emits tags periodically to provide downstream blocks information on the relationship between input and output items.

file_format: 1
