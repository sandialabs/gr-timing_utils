# auto-generated by grc.converter

id: timing_utils_edge_distance
label: Edge Distance
category: '[Sandia]/Timing Utilities'

parameters:
-   id: rate
    label: Sample Rate
    dtype: float
    default: samp_rate

inputs:
-   domain: stream
    dtype: float

templates:
    imports: import timing_utils
    make: timing_utils.edge_distance(${rate})

file_format: 1
