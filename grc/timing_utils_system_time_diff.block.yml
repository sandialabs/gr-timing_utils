# auto-generated by grc.converter

id: timing_utils_system_time_diff
label: System Time Difference
category: '[Sandia]/Timing Utilities'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [c, f, s, b]
    option_labels: [Complex, Float, Short, Byte]
    option_attributes:
        type: [complex, float, short, byte]
    hide: part
-   id: update_time
    label: Update Time?
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: output_diff
    label: Output Diff?
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: ${ type.type }

outputs:
-   domain: stream
    dtype: ${ type.type }
-   label: diff
    domain: stream
    dtype: float

templates:
    imports: |-
        import timing_utils
        import pmt
    make: timing_utils.system_time_diff_${type}(${update_time},${output_diff})

file_format: 1
